<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="channelInfo">
    <resultMap id="channelInfoResultMap" type="ChannelInfo">
        <id property="channelId" column="channel_id"/>
        <result property="channelTitle" column="channel_title"/>
        <result property="channelThumbnail" column="channel_thumbnail"/>
        <result property="subscriberCount" column="subscriber_count"/>
    </resultMap>

    <insert id="addChannelInfo" parameterType="ChannelInfo">
        INSERT INTO channel_info (channel_id, channel_title, channel_thumbnail, subscriber_count)
        VALUES (#{channelId}, #{channelTitle}, #{channelThumbnail}, #{subscriberCount})
    </insert>

    <update id="updateChannelInfo" parameterType="ChannelInfo">
        UPDATE channel_info
        SET channel_title = #{channelTitle},
        channel_thumbnail = #{channelThumbnail},
        subscriber_count = #{subscriberCount}
        WHERE channel_id = #{channelId}
    </update>

    <select id="getChannelInfoById" resultMap="channelInfoResultMap">
        SELECT * FROM channel_info WHERE channel_id = #{channelId}
    </select>
</mapper>