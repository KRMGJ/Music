<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:redis="http://www.springframework.org/schema/redis"
	xmlns:session="http://www.springframework.org/schema/session"
	xsi:schemaLocation=" 
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/redis http://www.springframework.org/schema/redis/spring-redis.xsd
        http://www.springframework.org/schema/session http://www.springframework.org/schema/session/spring-session.xsd">


	<!-- Spring MVC 설정 -->
	<mvc:annotation-driven />

	<mvc:resources mapping="/resources/**"
		location="/resources/" />

	<!-- Component 스캔 -->
	<context:component-scan
		base-package="com.example.music" />

	<context:property-placeholder location="classpath*:*.properties" />

	<beans:bean id="OAuthProperties" class="com.example.music.util.OAuthProperties"></beans:bean>

	<!-- View Resolver -->
	<beans:bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>

	<!-- 인터셉터 등록 -->
	<mvc:interceptors>
		<!-- LoginInterceptor -->
		<mvc:interceptor>
			<mvc:mapping path="/user/**" />
			<mvc:exclude-mapping path="/user/addUser" />
			<mvc:exclude-mapping path="/auth/**" />
			<mvc:exclude-mapping path="/common/**" />
			<mvc:exclude-mapping path="/" />
			<mvc:exclude-mapping path="/video/**" />
			<beans:bean
				class="com.example.music.interceptor.LoginInterceptor" />
		</mvc:interceptor>

		<!-- AdminInterceptor -->
		<mvc:interceptor>
			<mvc:mapping path="/admin/**" />
			<mvc:exclude-mapping path="/common/**" />
			<beans:bean
				class="com.example.music.interceptor.AdminInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors>

	<!-- password encoder -->
	<beans:bean id="passwordEncoder"
		class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
</beans:beans>
