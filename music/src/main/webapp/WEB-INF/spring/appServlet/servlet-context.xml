<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
        xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:beans="http://www.springframework.org/schema/beans"
        xmlns:mvc="http://www.springframework.org/schema/mvc"
        xmlns:context="http://www.springframework.org/schema/context"
        xsi:schemaLocation="
            http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
            http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">


    <!-- Spring MVC 설정 -->
    <mvc:annotation-driven />

    <!--<resources mapping="/resources/**" location="/resources/" />-->
    <!-- 정적 리소스 핸들러 -->
    <mvc:resources mapping="/**" location="classpath:/static/" cache-period="3600"/>

    <!-- Component 스캔 -->
    <context:component-scan base-package="com.example.music"/>

    <!-- View Resolver -->
    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/WEB-INF/views/"/>
        <beans:property name="suffix" value=".jsp"/>
    </beans:bean>

    <!-- 인터셉터 등록 -->
    <mvc:interceptors>
        <!-- LoginInterceptor -->
        <mvc:interceptor>
            <mvc:mapping path="/**"/>
            <mvc:exclude-mapping path="/user/addUser"/>
            <mvc:exclude-mapping path="/auth/**"/>
            <mvc:exclude-mapping path="/common/**"/>
            <mvc:exclude-mapping path="/"/>
            <mvc:exclude-mapping path="/video/**"/>
            <beans:bean class="com.example.music.interceptor.LoginInterceptor"/>
        </mvc:interceptor>

        <!-- AdminInterceptor -->
        <mvc:interceptor>
            <mvc:mapping path="/admin/**"/>
            <mvc:exclude-mapping path="/common/**"/>
            <beans:bean class="com.example.music.interceptor.AdminInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- password encoder -->
    <beans:bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>
</beans:beans>
